// src/config/pinStyles.js
// Custom pin styles for Chicago teams and affiliations

export const PIN_STYLES = {
  hotdog: {
    id: 'hotdog',
    name: 'Chicago Hot Dog',
    emoji: '🌭',
    colors: {
      primary: '#ef4444', // Red
      secondary: '#fbbf24', // Yellow
    },
    icon: '🌭',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%23ef4444"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E🌭%3C/text%3E%3C/svg%3E',
  },
  deepdish: {
    id: 'deepdish',
    name: 'Deep Dish Pizza',
    emoji: '🍕',
    colors: {
      primary: '#dc2626', // Pizza red
      secondary: '#fbbf24', // Cheese yellow
    },
    icon: '🍕',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%23dc2626"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E🍕%3C/text%3E%3C/svg%3E',
  },
  ltrain: {
    id: 'ltrain',
    name: 'L Train',
    emoji: '🚇',
    colors: {
      primary: '#1f2937', // Dark gray
      secondary: '#3b82f6', // Blue line
    },
    icon: '🚇',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%231f2937"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E🚇%3C/text%3E%3C/svg%3E',
  },
  skyline: {
    id: 'skyline',
    name: 'Chicago Skyline',
    emoji: '🏙️',
    colors: {
      primary: '#0ea5e9', // Sky blue
      secondary: '#64748b', // Gray
    },
    icon: '🏙️',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%230ea5e9"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E🏙️%3C/text%3E%3C/svg%3E',
  },
  bean: {
    id: 'bean',
    name: 'The Bean',
    emoji: '🫘',
    colors: {
      primary: '#94a3b8', // Silver/metallic
      secondary: '#cbd5e1', // Light silver
    },
    icon: '🫘',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%2394a3b8"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E🫘%3C/text%3E%3C/svg%3E',
  },
  chicagostar: {
    id: 'chicagostar',
    name: 'Chicago Star',
    emoji: '⭐',
    colors: {
      primary: '#B3DDF2', // Chicago flag light blue
      secondary: '#FF0000', // Red
    },
    icon: '⭐',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%23B3DDF2"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E⭐%3C/text%3E%3C/svg%3E',
  },
  bears: {
    id: 'bears',
    name: 'Bears',
    emoji: '🐻',
    colors: {
      primary: '#0B162A', // Navy blue
      secondary: '#C83803', // Orange
    },
    icon: '🐻',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%230B162A"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E🐻%3C/text%3E%3C/svg%3E',
  },
  bulls: {
    id: 'bulls',
    name: 'Bulls',
    emoji: '🐂',
    colors: {
      primary: '#CE1141', // Red
      secondary: '#000000', // Black
    },
    icon: '🐂',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%23CE1141"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E🐂%3C/text%3E%3C/svg%3E',
  },
  cubs: {
    id: 'cubs',
    name: 'Cubs',
    emoji: '⚾',
    colors: {
      primary: '#0E3386', // Cubs blue
      secondary: '#CC3433', // Red
    },
    icon: '⚾',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%230E3386"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E⚾%3C/text%3E%3C/svg%3E',
  },
  whitesox: {
    id: 'whitesox',
    name: 'White Sox',
    emoji: '⚪',
    colors: {
      primary: '#27251F', // Black
      secondary: '#C4CED4', // Silver
    },
    icon: '⚪',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%2327251F"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E⚪%3C/text%3E%3C/svg%3E',
  },
  blackhawks: {
    id: 'blackhawks',
    name: 'Blackhawks',
    emoji: '🏒',
    colors: {
      primary: '#CF0A2C', // Red
      secondary: '#000000', // Black
    },
    icon: '🏒',
    imageUrl: 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"%3E%3Ccircle cx="50" cy="50" r="45" fill="%23CF0A2C"/%3E%3Ctext x="50" y="70" font-size="60" text-anchor="middle"%3E🏒%3C/text%3E%3C/svg%3E',
  },
};

// Default style (hotdog - existing)
export const DEFAULT_PIN_STYLE = 'hotdog';

// Get all available styles as array
export const AVAILABLE_PIN_STYLES = Object.values(PIN_STYLES);

// Get style by id
export function getPinStyle(styleId) {
  return PIN_STYLES[styleId] || null;
}

// Get pin color based on style
export function getPinColor(pin) {
  if (pin.pinStyle && PIN_STYLES[pin.pinStyle]) {
    return PIN_STYLES[pin.pinStyle].colors.primary;
  }

  // Fall back to team color for backward compatibility
  if (pin.team === 'hotdog') return '#ef4444';
  if (pin.team === 'beef') return '#8b4513';
  return '#3b82f6'; // default blue
}

// Get pin icon/emoji based on style
export function getPinIcon(pin) {
  if (pin.pinStyle && PIN_STYLES[pin.pinStyle]) {
    return PIN_STYLES[pin.pinStyle].icon;
  }

  // Fall back to hotdog/beef for backward compatibility
  if (pin.hotdog) return '🌭';
  return '📍';
}

// Get pin image URL for custom marker rendering
export function getPinImageUrl(pin) {
  if (pin.pinStyle && PIN_STYLES[pin.pinStyle]) {
    return PIN_STYLES[pin.pinStyle].imageUrl;
  }

  // Return null for default rendering (emoji-based)
  return null;
}
